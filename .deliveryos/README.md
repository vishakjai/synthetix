# DeliveryOS Context Layer

- `schemas/`: canonical JSON Schema contracts for SCM, CP, HAB, context bundles, and agent I/O.
- `pipeline/`: orchestration and gate policy spec.
- `context/`: generated canonical artifact bundles per run/version.
- `feedback/`: human override events (JSON patch-style corrections).
